<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Brand - </title>
  <meta name="description" content="Simple landing page with cream background and sticky navigation (Home / About Us / Contact)." />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    :root{
      --text:#111;
      --muted:#333;
      --accent:#c19a6b; /* coffee-ish */
      --bg:#f5f5dc; /* cream */
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family:'Poppins',system-ui,-apple-system,Segoe UI,Roboto,Arial,
         'Noto Sans',sans-serif; color:var(--text); background:var(--bg);
      scroll-behavior:smooth;
    }

    /* NAV */
    .nav{
      position:fixed; inset-inline:0; top:0; z-index:50;
      backdrop-filter:saturate(120%) blur(8px);
      background:rgba(255,255,255,.6);
      border-bottom:1px solid rgba(0,0,0,.06);
    }
    .nav-inner{max-width:1100px; margin:auto; display:flex; align-items:center; gap:16px; padding:12px 20px}
    .brand{font-weight:700; letter-spacing:.5px}
    .brand a{color:var(--text); text-decoration:none}

    .menu{margin-left:auto; display:flex; gap:22px; align-items:center}
    .menu a{color:var(--text); text-decoration:none; font-weight:500; opacity:.9}
    .menu a:hover{opacity:1}

    .hamburger{display:none; margin-left:auto}
    .hamburger button{
      background:transparent; border:0; color:var(--text); font-size:28px; cursor:pointer
    }

    /* HERO */
    .hero{
      min-height:100vh; display:grid; place-items:center; text-align:center; position:relative;
      background:var(--bg);
    }
    .hero .content{max-width:900px; padding:8px 20px 64px}
    .eyebrow{color:var(--muted); text-transform:uppercase; letter-spacing:2.2px; font-size:12px}
    h1{font-size:clamp(34px, 5vw, 64px); margin:10px 0 14px; line-height:1.08}
    .lead{font-size:clamp(15px, 2vw, 19px); color:var(--muted)}

    .cta{margin-top:26px; display:flex; gap:12px; justify-content:center; flex-wrap:wrap}
    .btn{padding:12px 18px; border-radius:999px; border:1px solid rgba(0,0,0,.18); text-decoration:none; color:var(--text); font-weight:600}
    .btn.primary{background:var(--accent); border-color:transparent; color:#fff}
    .btn:hover{filter:brightness(1.04)}

    section{max-width:1000px; margin:0 auto; padding:84px 20px;}
    h2{font-size:clamp(26px,3.5vw,36px); margin:0 0 12px}
    p{color:var(--muted); line-height:1.7}
    .card{background:rgba(255,255,255,.7); border:1px solid rgba(0,0,0,.08); padding:20px; border-radius:18px}

    .contact-info{display:grid; gap:16px; margin-top:20px}
    .contact-item{display:flex; align-items:center; gap:10px; font-size:16px; color:var(--text)}
    .contact-item img{width:24px; height:24px}

    footer{border-top:1px solid rgba(0,0,0,.06); color:var(--muted); padding:28px 20px; text-align:center}

    @media (max-width: 820px){
      .menu{display:none}
      .hamburger{display:block}
      #drawer{position:fixed; inset:0 0 0 auto; width:min(78vw,320px); background:#fff; border-left:1px solid rgba(0,0,0,.08); transform:translateX(100%); transition:transform .28s ease; padding:18px}
      #drawer a{display:block; padding:14px 6px; color:var(--text); text-decoration:none; border-bottom:1px dashed rgba(0,0,0,.08)}
      #drawer.open{transform:none}
      #drawer .close{position:absolute; top:8px; right:12px; font-size:28px; background:transparent; border:0; color:var(--text); cursor:pointer}
    }
    .logo-bg {
  position: absolute;
  left: 50%;
  top: 50%;
  width:1200px;   /* ปรับขนาดตามต้องการ */
  height: 550px;   /* ปรับขนาดตามต้องการ */
  transform: translate(-50%, -50%);
  opacity: 0.13;  /* ปรับความจาง */
  pointer-events: none;
  z-index: 0;
}
.hero .content > *:not(.logo-bg) {
  position: relative;
  z-index: 1;
}
  </style>
</head>
<body>
  <nav class="nav">
    <div class="nav-inner">
      <div class="brand"><a href="#home">Your Brand</a></div>
      <div class="menu" aria-label="Primary">
        <a href="#home">Home</a>
        <a href="#about">About Us</a>
        <a href="#contact">Contact</a>
      </div>
      <div class="hamburger">
        <button aria-label="Open menu" onclick="document.getElementById('drawer').classList.add('open')">☰</button>
      </div>
    </div>
  </nav>

  <aside id="drawer" aria-hidden="true">
    <button class="close" aria-label="Close menu" onclick="document.getElementById('drawer').classList.remove('open')">✕</button>
    <a href="#home" onclick="drawerClose()">Home</a>
    <a href="#about" onclick="drawerClose()">About Us</a>
    <a href="#contact" onclick="drawerClose()">Contact</a>
  </aside>

 
  <header id="home" class="hero" role="banner">
  <div class="content" style="position:relative;">
    <img src="pic/logo.png" alt="Logo" class="logo-bg" />
    <div class="eyebrow">SERVICE • MAINTENANCE • SUPPORT</div>
    <h1>ยินดีต้อนรับเข้าสู่เว็ปไซต์</h1>
    <p class="lead">xxxxxxxxxxxxxxxxxxxxxxxxx</p>
    <div class="cta">
      <a class="btn primary" href="#contact">Book Now</a>
      <a class="btn" href="#about">Learn More</a>
    </div>
  </div>
</header>

  <section id="about">
    <div class="card">
      <h2>About Us</h2>
      <p>บริการด้วยทีมงานมืออาชีพเราพร้อมให้บริการในกรุงเทพฯ และปริมณฑล ด้วยความใส่ใจในทุกรายละเอียด </p>
    </div>
  </section>
<section id="contact">
  <div class="card">
    <h2>Contact us</h2>
    <div style="display:flex; flex-direction:column; gap:14px; margin-top:14px">
      <div style="display:flex; align-items:center; gap:10px">
        📱 <span>082-881-1527</span>
      </div>
      <div style="display:flex; align-items:center; gap:10px">
        ✉️ <span>sakarin.sittkrua@gmail.com</span>
      </div>
    </div>
  </div>
</section>

  <footer>
    © <span id="year"></span> Your Brand • All rights reserved
  </footer>

  <script>
    function drawerClose(){ document.getElementById('drawer').classList.remove('open'); }
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
